{% include "base.html.twig" %}

{% block content %}
    <h1>Add User</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Create User</button>
    {{ form_end(form) }}
{% endblock %}
